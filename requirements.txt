# Tweet Sentiment Analysis - Python dependencies
#
# Recommended: Python 3.10–3.12 (matches the notebook runtime metadata).
# Python 3.13 is supported below via NumPy 2.x wheels (NumPy 1.x has no 3.13 wheels
# and will fail to install unless you compile from source with MSVC).
#
# After installing, fetch NLTK data once (Part 1):
#   python -c "import nltk; nltk.download('stopwords'); nltk.download('wordnet'); nltk.download('omw-1.4')"

# ----- Core data / numerics -----
numpy>=1.23,<2.0; python_version < "3.13"
numpy>=2.1,<3.0; python_version >= "3.13"
pandas>=1.5
scipy>=1.10

# ----- Plotting -----
matplotlib>=3.4,<4.0
seaborn>=0.12,<1.0
wordcloud>=1.9,<2.0

# ----- NLP -----
nltk>=3.8,<4.0

# ----- Classical ML (Part 2-1) -----
scikit-learn>=1.2,<2.0

# ----- Deep learning (Part 2-2) -----
# Notebooks use: from keras import models, layers, regularizers
tensorflow>=2.10
keras>=2.10

# ----- Notebook environment -----
jupyter>=1.0
notebook>=6.5
ipykernel>=6.20
